- name: create congig file
  ansible.builtin.template:
    src: job.xml
    dest: /tmp/job.xml
    remote_src: yes


- name: Create a jenkins job 
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/job.xml') }}"
    name: "{{item}}"
    password: admin123
    url: http://172.31.94.184:8080
    user: admin
    loop:
      - frontend
      - catalogue
      - cart

